[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "repowelcome"
version = "0.1.0"
description = "Audit how ready an open-source repository is for new contributors"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Kwondh0321" }]
keywords = ["open-source", "contributors", "repository", "community", "audit"]

[project.optional-dependencies]
dev = ["ruff==0.16.4"]

[project.urls]
Homepage = "https://github.com/Kwondh0321/repowelcome"
Repository = "https://github.com/Kwondh0321/repowelcome.git"
Issues = "https://github.com/Kwondh0321/repowelcome/issues"

[project.scripts]
repowelcome = "repowelcome.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
